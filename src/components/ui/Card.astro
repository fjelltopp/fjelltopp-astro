---
const { isUnderline } = Astro.props;
---

{
  isUnderline ? (
    <div class="border-dark h-full rounded-[45px] border transition-shadow hover:shadow-lg focus:shadow-lg focus:outline-none">
      <slot />
    </div>
  ) : (
    <div class="rounded-[45px]">
      <slot />
    </div>
  )
}
