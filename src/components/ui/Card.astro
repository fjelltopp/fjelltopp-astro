---
const { isUnderline } = Astro.props;
---

{
  isUnderline ? (
    <div class="border-dark rounded-[45px] border h-full transition-shadow hover:shadow-lg">
      <slot />
    </div>
  ) : (
    <div class="rounded-[45px]">
      <slot />
    </div>
  )
}
